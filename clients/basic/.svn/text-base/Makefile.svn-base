PROJECT = client

CC = clang
INCLUDE = ../../
HLIB = ../../hlib/hlib.a
CFLAGS = -Wall -std=c99 -pedantic -g -I$(INCLUDE)

OBJECTS = client.o

all : $(PROJECT)

.c.o :
	$(CC) -c $(CFLAGS) $<

$(PROJECT) : $(OBJECTS)
	$(CC) $(OBJECTS) -o $(PROJECT) $(HLIB)


clean:
	rm -f *.o $(PROJECT)
